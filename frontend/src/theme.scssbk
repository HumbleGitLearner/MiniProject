//@use '@angular/material/core/theming' as mat;
//@use "@angular/material" as mat;

@include mat.core();

$primary: mat.define-palette($pink-palette, 600);
$accent: mat.define-palette($pink-palette, 100);

$theme: mat.define-light-theme(
  (
    color: (
      primary: $primary,
      accent: $accent,
    )
  )
);

@include mat.all-component-themes($theme);

body {
  font-family: Roboto, Arial, Helvetica, sans-serif;
}

$primary-color: mat.get-color-from-palette($primary);
$background-color: rgb(237 237 237);
$secondary-background-color: #f8f8f8;
$tool-color: $primary;